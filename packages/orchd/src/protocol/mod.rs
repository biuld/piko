// ---- Protocol: wire/config DTOs ----
//
// Re-exports all protocol types from piko_protocol.
// The protocol module provides the shared vocabulary between
// hostd and orchd for commands, events, snapshots, messages,
// model config, and sessions.

pub use piko_protocol::*;
