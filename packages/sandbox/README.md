# piko-sandbox

Standalone, fail-closed supervisor used by piko runtime components. Historical
policy notes live in
[`docs/archive/sandbox-design-analysis.md`](../../docs/archive/sandbox-design-analysis.md);
revalidate them against current hostd/orchd code before implementation work.
