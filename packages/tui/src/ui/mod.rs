pub mod components;
pub mod interaction;
pub mod interaction_hints;
/// Terminal line layout (column math + left/right row paint). Not flex geometry.
pub mod line_layout;
/// Column-aware span wrapping + prefixed rows (gutter / labeled text).
pub mod line_wrap;
