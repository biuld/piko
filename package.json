{
  "name": "piko",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "tsc -b --pretty false",
    "clean": "rm -rf packages/*/dist",
    "build": "tsc -b",
    "test": "npm run check"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "chalk": "^5.6.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
