{
  "name": "piko",
  "private": true,
  "packageManager": "bun@1.3.14",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "bun run build && bun packages/cli/bin/piko",
    "piko": "bun packages/cli/bin/piko",
    "check": "biome check && tsc -b --pretty false && tsc -p tsconfig.test.json",
    "fmt": "biome check --fix",
    "clean": "rm -rf packages/*/dist dist-bundle",
    "build": "bun run fmt && tsc -b",
    "compile": "bun run build && bun run scripts/build-binary.ts && bun build ./dist-bundle/cli.js --compile --no-compile-autoload-bunfig --outfile dist-bundle/piko",
    "install-bin": "cp dist-bundle/piko /usr/local/bin/piko",
    "test": "bun test --preload ./packages/host-runtime/test/setup.ts",
    "check:all": "bun run check && bun run test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
